local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- UI Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PetSpawnerUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 420, 0, 300)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 35, 30)
MainFrame.Parent = ScreenGui
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.Text = "Shin Script V1"
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(160, 255, 160)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.Parent = MainFrame

local PetName = Instance.new("TextBox")
PetName.Size = UDim2.new(0.5, -10, 0, 25)
PetName.Position = UDim2.new(0, 10, 0, 40)
PetName.PlaceholderText = "Pet Name"
PetName.Text = ""
PetName.TextSize = 14
PetName.TextColor3 = Color3.fromRGB(220, 255, 220)
PetName.BackgroundColor3 = Color3.fromRGB(45, 55, 45)
PetName.Font = Enum.Font.Gotham
PetName.Parent = MainFrame

local WeightSlider = Instance.new("TextBox")
WeightSlider.Size = UDim2.new(0.5, -10, 0, 25)
WeightSlider.Position = UDim2.new(0.5, 0, 0, 40)
WeightSlider.PlaceholderText = "Weight (kg)"
WeightSlider.Text = ""
WeightSlider.TextSize = 14
WeightSlider.TextColor3 = Color3.fromRGB(220, 255, 220)
WeightSlider.BackgroundColor3 = Color3.fromRGB(45, 55, 45)
WeightSlider.Font = Enum.Font.Gotham
WeightSlider.Parent = MainFrame

local AgeSlider = Instance.new("TextBox")
AgeSlider.Size = UDim2.new(0.5, -10, 0, 25)
AgeSlider.Position = UDim2.new(0, 10, 0, 75)
AgeSlider.PlaceholderText = "Age (years)"
AgeSlider.Text = ""
AgeSlider.TextSize = 14
AgeSlider.TextColor3 = Color3.fromRGB(220, 255, 220)
AgeSlider.BackgroundColor3 = Color3.fromRGB(45, 55, 45)
AgeSlider.Font = Enum.Font.Gotham
AgeSlider.Parent = MainFrame

local SpawnButton = Instance.new("TextButton")
SpawnButton.Size = UDim2.new(0.5, -10, 0, 25)
SpawnButton.Position = UDim2.new(0.5, 0, 0, 75)
SpawnButton.Text = "Spawn Pet"
SpawnButton.TextSize = 14
SpawnButton.BackgroundColor3 = Color3.fromRGB(50, 100, 50)
SpawnButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SpawnButton.Font = Enum.Font.GothamBold
SpawnButton.Parent = MainFrame

local SpawnedListScrolling = Instance.new("Frame")
SpawnedListScrolling.Size = UDim2.new(1, -20, 0, 150)
SpawnedListScrolling.Position = UDim2.new(0, 10, 0, 110)
SpawnedListScrolling.BackgroundColor3 = Color3.fromRGB(25, 30, 25)
SpawnedListScrolling.Parent = MainFrame

-- Username Generator\local usernameData = {
	western = {
		"xProGamer", "ShadowSlayer", "EpicBuilder", "NoobMaster", "OGPlayer", 
		"SweatyTryhard", "VoidWalker", "LootGoblin", "ToxicTeammate", "CarryMePlz",
		"TryhardTimmy", "SweatySteve", "BuilderBob", "ProPlayer123", "NoobKiller",
		"AimbotAndy", "ToxicTerry", "LagSpiker", "ReportMePlz", "AdoptMeRich"
	},
	japanese = {
		"龍神_Ryujin", "桜_Sakura", "忍者_Ninja", "神_Kami", "鬼殺隊_DemonSlayer",
		"卍解_Bankai", "新幹線_Shinkansen", "東京_Tokyo", "大阪_Osaka", "富士山_Fuji",
		"アニメ_Anime", "漫画_Manga", "カワイイ_Kawaii", "バカ_Baka", "スシ_Sushi",
		"ラーメン_Ramen", "サムライ_Samurai", "ニンジャ_Ninja", "オタク_Otaku", "ガンダム_Gundam"
	},
	filipino = {
		"Pogiako", "GandaMoNaman", "LodiTol", "Bossing", "SanaAll",
		"Petmalu", "Werpa", "SanaOl", "Charot", "Hahaha",
		"EdiWow", "Awit", "SigeNa", "Luh", "AyokoNa",
		"BidaBida", "SanaAllRich", "AdikYarn", "GGWP", "EZWin"
	},
	otherAsian = {
		"XiaoLong", "WeiWei", "Kimchi", "BTS_ARMY", "KPopFan",
		"DimSum", "BubbleTea", "HongBao", "Lucky8", "DragonZ",
		"PandaLove", "SushiLover", "Annyeong", "Kamsa", "Waii"
	},
	special = {
		"★DarkKnight★", "~Fluffy~", "⚡Speedster⚡", "❄Frosty❄", "♛King♛",
		"♔ProPlayer♔", "☯Balance☯", "✿Flower✿", "♫Music♫", "∞Infinity∞"
	}
}

local function generateRealisticUsername()
	local usernameType = math.random(1, 5)
	local nameList = usernameType == 1 and usernameData.western
		or usernameType == 2 and usernameData.japanese
		or usernameType == 3 and usernameData.filipino
		or usernameType == 4 and usernameData.otherAsian
		or usernameData.special

	local baseName = nameList[math.random(1, #nameList)]

	if math.random(1, 100) <= 70 then
		local numbers = math.random(1, 100) <= 30 and tostring(math.random(2005, 2025)) or tostring(math.random(1, 999))
		if math.random(1, 100) <= 40 then
			return baseName .. numbers
		else
			return baseName .. "_" .. numbers
		end
	end

	return baseName
end

-- Notification Spawner
local function createNotification(username, petName, weight, age)
	local notificationFrame = Instance.new("Frame")
	notificationFrame.Name = "Notification"
	notificationFrame.Size = UDim2.new(1, 0, 0, 60)
	notificationFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
	notificationFrame.BackgroundTransparency = 1
	notificationFrame.Position = UDim2.new(0, 0, 0, 10)
	notificationFrame.Parent = SpawnedListScrolling

	local UICorner = Instance.new("UICorner")
	UICorner.CornerRadius = UDim.new(0, 5)
	UICorner.Parent = notificationFrame

	local userLabel = Instance.new("TextLabel")
	userLabel.Size = UDim2.new(1, -10, 0, 20)
	userLabel.Position = UDim2.new(0, 5, 0, 5)
	userLabel.BackgroundTransparency = 1
	userLabel.Text = "User: " .. username
	userLabel.TextColor3 = Color3.fromRGB(180, 220, 255)
	userLabel.TextXAlignment = Enum.TextXAlignment.Left
	userLabel.Font = Enum.Font.GothamSemibold
	userLabel.TextSize = 12
	userLabel.TextTransparency = 1
	userLabel.Parent = notificationFrame

	local petLabel = Instance.new("TextLabel")
	petLabel.Size = UDim2.new(1, -10, 0, 20)
	petLabel.Position = UDim2.new(0, 5, 0, 25)
	petLabel.BackgroundTransparency = 1
	petLabel.Text = "Spawned: " .. petName .. " (" .. string.format("%.1f", weight) .. "kg, " .. age .. "yrs)"
	petLabel.TextColor3 = Color3.fromRGB(220, 220, 180)
	petLabel.TextXAlignment = Enum.TextXAlignment.Left
	petLabel.Font = Enum.Font.Gotham
	petLabel.TextSize = 12
	petLabel.TextTransparency = 1
	petLabel.Parent = notificationFrame

	local timeLabel = Instance.new("TextLabel")
	timeLabel.Size = UDim2.new(1, -10, 0, 15)
	timeLabel.Position = UDim2.new(0, 5, 0, 45)
	timeLabel.BackgroundTransparency = 1
	timeLabel.Text = "Just now"
	timeLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
	timeLabel.TextXAlignment = Enum.TextXAlignment.Left
	timeLabel.Font = Enum.Font.Gotham
	timeLabel.TextSize = 10
	timeLabel.TextTransparency = 1
	timeLabel.Parent = notificationFrame

	TweenService:Create(notificationFrame, TweenInfo.new(0.4), {BackgroundTransparency = 0}):Play()
	TweenService:Create(userLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
	TweenService:Create(petLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
	TweenService:Create(timeLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()

	local swayConnection = RunService.Heartbeat:Connect(function()
		if notificationFrame and notificationFrame.Parent then
			notificationFrame.Position = UDim2.new(math.sin(os.clock() * 2.5) * 0.02, 0, 0, 10)
		end
	end)

	notificationFrame.Destroying:Connect(function()
		if swayConnection then swayConnection:Disconnect() end
	end)

	local count = 0
	local oldest = nil
	for _, child in ipairs(SpawnedListScrolling:GetChildren()) do
		if child:IsA("Frame") and child.Name == "Notification" then
			count += 1
			if not oldest then oldest = child end
		end
	end

	if count > 20 and oldest then
		TweenService:Create(oldest, TweenInfo.new(0.3), {
			BackgroundTransparency = 1,
			Position = UDim2.new(0, 0, 0, -20)
		}):Play()
		oldest:Destroy()
	end
end

-- Manual spawn button
SpawnButton.MouseButton1Click:Connect(function()
	local pet = PetName.Text ~= "" and PetName.Text or "MysteryPet"
	local weight = tonumber(WeightSlider.Text) or math.random(10, 300) / 10
	local age = tonumber(AgeSlider.Text) or math.random(1, 50)
	local username = generateRealisticUsername()
	createNotification(username, pet, weight, age)
end)

-- Auto spawn random pets
local petTypes = {
	"Kitsune", "Raccoon", "Disco Bee", "Corrupted Kitsune", "Raiju",
	"T-Rex", "Spinosaurus", "Dragonfly", "Butterfly", "Mimic Octopus", "Queen Bee"
}

coroutine.wrap(function()
	while true do
		wait(math.random(2, 5))
		local username = generateRealisticUsername()
		local pet = petTypes[math.random(1, #petTypes)]
		local weight = math.random(10, 5000) / 10
		local age = math.random(1, 100)
		createNotification(username, pet, weight, age)
	end
end)()
